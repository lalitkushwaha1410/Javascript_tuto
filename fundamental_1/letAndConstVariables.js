// declare a variable with let

var fname = "lalit";

console.log(fname);

var fname = "akansha";

console.log(fname);

//let variable cant be redeclared
 let sname = "kushwaha";
console.log(sname);
  sname = "maurya"; // Cannot redeclare block-scoped variable 'sname'

// var - can be re-declared , re-assign , hoisted , function scope
// let - cant be redeclared , can be re-assigned , not hoisted , block scope
// const - cant be re-declared, cant be re-assigned, not hoisted, block scope